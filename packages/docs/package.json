{
  "name": "docs",
  "private": true,
  "scripts": {
    "format": "prettier --ignore-path ../../.prettierignore './**/*.md' --check",
    "format:fix": "prettier --ignore-path ../../.prettierignore './**/*.md' --write"
  }
}
